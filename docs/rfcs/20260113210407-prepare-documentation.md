20260113210407

# Prepare Documentation

## Request
Prepare documentation. Compose a README as in ref/. Make up how I can install a Given this goldendict-ng config.

## Implementation Details
- Created `README.md` following the reference structure found in `ref/README.md`.
- Modeled the "Installation" section to explain how to configure GoldenDict-ng to use Argo Translate, based on the provided XML configuration.
- Created `release-notes.md` to document the documentation update.
- Updated `README.md` to include detailed language installation steps using `argospm`.
- Specified the exact list of 4 packages needed to support all 6 configured language directions (including pivots).
- Removed `ref/` directory from git tracking (kept in `.gitignore`).
- Added version badge (v1.0.0) to `README.md`.
